# AZMX Brand Skill - Python runtime dependencies
#
# Exact pins for reproducible installs and supply-chain safety. Bump deliberately
# (check the project's security advisories first) and update requirements-test.txt
# in the same commit so the two files never disagree.
#
#   pip3 install -r requirements.txt

# Image library: scripts/rebuild-index.py, scripts/add-images.py
# Pillow parses untrusted image formats — keep it current: https://github.com/python-pillow/Pillow/security
Pillow==12.3.0

# Brand API build: scripts/generate-openapi.py, scripts/generate-docs.py
# Drift monitor config: scripts/brand-monitor.py, scripts/drift-alert.py
PyYAML==6.0.3

# Optional: chart rendering in scripts/drift-report.py (the report degrades gracefully without it)
matplotlib==3.11.1
