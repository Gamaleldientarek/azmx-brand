.DS_Store
scripts/node_modules/

# Auto Claude data directory
.auto-claude/

# Verification files
output.css
verify-contrast.mjs
test-contrast.html
test_contrast.py

# Python virtual environment and test artifacts
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Test coverage
.coverage
.pytest_cache/
htmlcov/
.tox/
coverage.xml
*.cover

# Test reports
test-results/
junit.xml
.test_report.json

# Auto Claude generated files
.auto-claude-security.json
.auto-claude-status
.claude_settings.json
.worktrees/
.security-key
logs/security/

# Node.js test coverage reports
scripts/coverage/

# Brand drift detection database
.brand-drift.db

# RTL validation scratch file
test-rtl-validation.html

# Compliance report history
.brand-reports/
reports/

# Local drift-monitor output (.brand-monitor.yml is committed as a template; secrets go in env vars)
brand-monitor.log
brand-drift-report.html

# Token-migration run output
migration-map.json
migration-report.md
migration.json
migration-manual-review.json

# Vitest scratch output
tests/temp/

# -----------------------------------------------------------------------------
# Secrets & credentials — never commit these (Brand API, MCP server, marketplace
# integrations will all need tokens; keep them in the environment or in ignored files)
# -----------------------------------------------------------------------------
.env
.env.*
!.env.example
.envrc
*.env
*.pem
*.key
*.p12
*.pfx
*.keystore
credentials.json
*-credentials.json
*-service-account*.json
service-account*.json
secrets.yml
secrets.yaml
.secrets
.npmrc
.yarnrc
.yarnrc.yml
.aws/
.gcp/

# Brand API build intermediates (api/ and api-docs/ are the committed outputs)
data/
_site/

# Agent scratch output (Auto-Claude verification scripts and reports)
.claude
.claude_settings.json
VERIFICATION_REPORT.md
verify_*.py
test_csp_hash.py

# Brand image JPEGs live in the azmx-brand-cdn repository (served from jsDelivr;
# see scripts/image-meta.json). A local working copy for offline use is fine,
# but must never be committed here again.
assets/images/
